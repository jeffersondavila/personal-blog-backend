"""Utilidades ejecutables del repositorio.

Es un paquete, y no un directorio suelto, para que `mypy` resuelva cada archivo
bajo un unico nombre de modulo y para que las pruebas puedan importarlos por su
ruta canonica (`scripts.seed_local`).
"""
