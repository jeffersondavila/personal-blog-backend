# ---------------------------------------------------------------------------
# personal-blog-backend — dependencias de ejecucion
#
# Versiones fijadas de forma exacta (requisito S-09). Debe coincidir con
# `[project].dependencies` de pyproject.toml: este archivo existe porque el
# Dockerfile instala desde el, sin necesitar el proyecto completo.
#
# El bloqueo de las dependencias transitivas, con hashes y resuelto en Linux,
# corresponde a la CI (`Task/020`).
#
# Instalacion:
#   pip install -r requirements.txt
# ---------------------------------------------------------------------------

fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4
pydantic-settings==2.14.2
sqlalchemy==2.0.51
psycopg[binary]==3.3.4
alembic==1.18.5
boto3==1.43.82
pillow==12.3.0
